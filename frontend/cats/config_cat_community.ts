import anon2Image from '../images/anon2.jpeg'
import type { CatCornerConfig } from './types'

export const configCatCommunity: CatCornerConfig = {
  title: 'Anon the Community Guide',
  description: 'Browse public workflows here, then search, filter, inspect, and save the ones worth reusing.',
  detailText: 'Use the search bar for partial names or tool names, narrow the list with tool filters, open a workflow card to inspect it, and save a copy into your own workspace when you want to adapt it.',
  mediaSrc: anon2Image,
  mediaAlt: 'Community guide cat',
}
