apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cassie-pvc
spec:
  accessModes:
    - ReadWriteOnce 
  resources:
    requests:
      storage: 2Gi
  storageClassName: standard # Kind's default storage class
